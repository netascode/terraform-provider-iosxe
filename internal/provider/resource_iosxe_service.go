// Code generated by "gen/generator.go"; DO NOT EDIT.

package provider

import (
	"context"
	"fmt"

	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/tfsdk"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-go/tftypes"
	"github.com/hashicorp/terraform-plugin-log/tflog"
	"github.com/netascode/terraform-provider-iosxe/internal/provider/helpers"
)

type resourceServiceType struct{}

func (t resourceServiceType) GetSchema(ctx context.Context) (tfsdk.Schema, diag.Diagnostics) {
	return tfsdk.Schema{
		// This description is used by the documentation generator and the language server.
		MarkdownDescription: "This resource can manage the Service configuration.",

		Attributes: map[string]tfsdk.Attribute{
			"device": {
				MarkdownDescription: "A device name from the provider configuration.",
				Type:                types.StringType,
				Optional:            true,
			},
			"id": {
				MarkdownDescription: "The path of the object.",
				Type:                types.StringType,
				Computed:            true,
				PlanModifiers: tfsdk.AttributePlanModifiers{
					tfsdk.UseStateForUnknown(),
				},
			},
			"pad": {
				MarkdownDescription: helpers.NewAttributeDescription("Enable PAD commands").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"password_encryption": {
				MarkdownDescription: helpers.NewAttributeDescription("Encrypt system passwords").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"password_recovery": {
				MarkdownDescription: helpers.NewAttributeDescription("Enable password recovery").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps": {
				MarkdownDescription: helpers.NewAttributeDescription("Timestamp debug/log messages").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_debug": {
				MarkdownDescription: helpers.NewAttributeDescription("Timestamp debug messages").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_debug_datetime": {
				MarkdownDescription: helpers.NewAttributeDescription("Timestamp with date and time").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_debug_datetime_msec": {
				MarkdownDescription: helpers.NewAttributeDescription("Include milliseconds in timestamp").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_debug_datetime_localtime": {
				MarkdownDescription: helpers.NewAttributeDescription("Use local time zone for timestamps").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_debug_datetime_show_timezone": {
				MarkdownDescription: helpers.NewAttributeDescription("Add time zone information to timestamp").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_debug_datetime_year": {
				MarkdownDescription: helpers.NewAttributeDescription("Include year in timestamp").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_debug_uptime": {
				MarkdownDescription: helpers.NewAttributeDescription("Timestamp with system uptime").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_log": {
				MarkdownDescription: helpers.NewAttributeDescription("Timestamp log messages").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_log_datetime": {
				MarkdownDescription: helpers.NewAttributeDescription("Timestamp with date and time").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_log_datetime_msec": {
				MarkdownDescription: helpers.NewAttributeDescription("Include milliseconds in timestamp").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_log_datetime_localtime": {
				MarkdownDescription: helpers.NewAttributeDescription("Use local time zone for timestamps").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_log_datetime_show_timezone": {
				MarkdownDescription: helpers.NewAttributeDescription("Add time zone information to timestamp").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_log_datetime_year": {
				MarkdownDescription: helpers.NewAttributeDescription("Include year in timestamp").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"timestamps_log_uptime": {
				MarkdownDescription: helpers.NewAttributeDescription("Timestamp with system uptime").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"dhcp": {
				MarkdownDescription: helpers.NewAttributeDescription("Enable DHCP server and relay agent").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"tcp_keepalives_in": {
				MarkdownDescription: helpers.NewAttributeDescription("Generate keepalives on idle incoming network connections").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
			"tcp_keepalives_out": {
				MarkdownDescription: helpers.NewAttributeDescription("Generate keepalives on idle outgoing network connections").String,
				Type:                types.BoolType,
				Optional:            true,
				Computed:            true,
			},
		},
	}, nil
}

func (t resourceServiceType) NewResource(ctx context.Context, in tfsdk.Provider) (tfsdk.Resource, diag.Diagnostics) {
	provider, diags := convertProviderType(in)

	return resourceService{
		provider: provider,
	}, diags
}

type resourceService struct {
	provider provider
}

func (r resourceService) Create(ctx context.Context, req tfsdk.CreateResourceRequest, resp *tfsdk.CreateResourceResponse) {
	var plan Service

	// Read plan
	diags := req.Plan.Get(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	tflog.Debug(ctx, fmt.Sprintf("%s: Beginning Create", plan.getPath()))

	// Create object
	body := plan.toBody()

	res, err := r.provider.clients[plan.Device.Value].PatchData(plan.getPathShort(), body)
	if len(res.Errors.Error) > 0 && res.Errors.Error[0].ErrorMessage == "patch to a nonexistent resource" {
		_, err = r.provider.clients[plan.Device.Value].PutData(plan.getPath(), body)
	}
	if err != nil {
		resp.Diagnostics.AddError("Client Error", fmt.Sprintf("Failed to configure object (PATCH), got error: %s", err))
		return
	}

	emptyLeafsDelete := plan.getEmptyLeafsDelete()
	tflog.Debug(ctx, fmt.Sprintf("List of empty leafs to delete: %+v", emptyLeafsDelete))

	for _, i := range emptyLeafsDelete {
		res, err := r.provider.clients[plan.Device.Value].DeleteData(i)
		if err != nil && res.StatusCode != 404 {
			resp.Diagnostics.AddError("Client Error", fmt.Sprintf("Failed to delete object, got error: %s", err))
			return
		}
	}

	plan.setUnknownValues()

	plan.Id = types.String{Value: plan.getPath()}

	tflog.Debug(ctx, fmt.Sprintf("%s: Create finished successfully", plan.getPath()))

	diags = resp.State.Set(ctx, &plan)
	resp.Diagnostics.Append(diags...)
}

func (r resourceService) Read(ctx context.Context, req tfsdk.ReadResourceRequest, resp *tfsdk.ReadResourceResponse) {
	var state Service

	// Read state
	diags := req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	tflog.Debug(ctx, fmt.Sprintf("%s: Beginning Read", state.Id.Value))

	res, err := r.provider.clients[state.Device.Value].GetData(state.Id.Value)
	if res.StatusCode == 404 {
		state = Service{Device: state.Device, Id: state.Id}
	} else {
		if err != nil {
			resp.Diagnostics.AddError("Client Error", fmt.Sprintf("Failed to retrieve object, got error: %s", err))
			return
		}

		state.updateFromBody(res.Res)
	}

	tflog.Debug(ctx, fmt.Sprintf("%s: Read finished successfully", state.Id.Value))

	diags = resp.State.Set(ctx, &state)
	resp.Diagnostics.Append(diags...)
}

func (r resourceService) Update(ctx context.Context, req tfsdk.UpdateResourceRequest, resp *tfsdk.UpdateResourceResponse) {
	var plan, state Service

	// Read plan
	diags := req.Plan.Get(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Read state
	diags = req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	tflog.Debug(ctx, fmt.Sprintf("%s: Beginning Update", plan.Id.Value))

	body := plan.toBody()
	res, err := r.provider.clients[plan.Device.Value].PatchData(plan.getPathShort(), body)
	if len(res.Errors.Error) > 0 && res.Errors.Error[0].ErrorMessage == "patch to a nonexistent resource" {
		_, err = r.provider.clients[plan.Device.Value].PutData(plan.getPath(), body)
	}
	if err != nil {
		resp.Diagnostics.AddError("Client Error", fmt.Sprintf("Failed to configure object (PATCH), got error: %s", err))
		return
	}

	plan.setUnknownValues()

	deletedListItems := plan.getDeletedListItems(state)
	tflog.Debug(ctx, fmt.Sprintf("List items to delete: %+v", deletedListItems))

	for _, i := range deletedListItems {
		res, err := r.provider.clients[state.Device.Value].DeleteData(i)
		if err != nil && res.StatusCode != 404 {
			resp.Diagnostics.AddError("Client Error", fmt.Sprintf("Failed to delete object, got error: %s", err))
			return
		}
	}

	emptyLeafsDelete := plan.getEmptyLeafsDelete()
	tflog.Debug(ctx, fmt.Sprintf("List of empty leafs to delete: %+v", emptyLeafsDelete))

	for _, i := range emptyLeafsDelete {
		res, err := r.provider.clients[plan.Device.Value].DeleteData(i)
		if err != nil && res.StatusCode != 404 {
			resp.Diagnostics.AddError("Client Error", fmt.Sprintf("Failed to delete object, got error: %s", err))
			return
		}
	}

	tflog.Debug(ctx, fmt.Sprintf("%s: Update finished successfully", plan.Id.Value))

	diags = resp.State.Set(ctx, &plan)
	resp.Diagnostics.Append(diags...)
}

func (r resourceService) Delete(ctx context.Context, req tfsdk.DeleteResourceRequest, resp *tfsdk.DeleteResourceResponse) {
	var state Service

	// Read state
	diags := req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	tflog.Debug(ctx, fmt.Sprintf("%s: Beginning Delete", state.Id.Value))

	res, err := r.provider.clients[state.Device.Value].DeleteData(state.Id.Value)
	if err != nil && res.StatusCode != 404 && res.StatusCode != 400 {
		resp.Diagnostics.AddError("Client Error", fmt.Sprintf("Failed to delete object, got error: %s", err))
		return
	}

	tflog.Debug(ctx, fmt.Sprintf("%s: Delete finished successfully", state.Id.Value))

	resp.State.RemoveResource(ctx)
}

func (r resourceService) ImportState(ctx context.Context, req tfsdk.ImportResourceStateRequest, resp *tfsdk.ImportResourceStateResponse) {
	tfsdk.ResourceImportStatePassthroughID(ctx, tftypes.NewAttributePath().WithAttributeName("id"), req, resp)
}
